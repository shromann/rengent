# rengent

## UV project setup

1. Install `uv` from [astral.sh](https://astral.sh/uv) or your package manager.


2. Install or sync dependencies:

```bash
uv sync
```

3. After `uv sync` finishes, use the generated lockfile and vendor data to run your project.
